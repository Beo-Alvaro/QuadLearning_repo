# Backend environment variables - EXAMPLE FILE
# Rename to .env and update with your actual values

# Node environment
NODE_ENV=production

# Port for the server to run on (Render will set this automatically)
PORT=5000

# MongoDB connection URI
MONGO_URI=mongodb+srv://username:password@cluster.mongodb.net/dbname

# JWT secret for token generation and verification
JWT_SECRET=your-jwt-secret-key-here

# Encryption key (same as frontend VITE_ENCRYPTION_KEY)
VITE_ENCRYPTION_KEY=your-encryption-key-here

# Frontend URL for CORS (your Render static site URL)
FRONTEND_URL=https://your-frontend-name.onrender.com 