# Production environment variables - EXAMPLE FILE
# Rename to .env.production and update with your actual values

# API URL - point this to your deployed backend URL on Render
VITE_API_URL=https://your-backend-service-name.onrender.com

# Encryption key for password encryption (use the same key as in backend)
VITE_ENCRYPTION_KEY=your-encryption-key-here 